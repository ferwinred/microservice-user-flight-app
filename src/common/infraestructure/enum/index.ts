export enum Dialect {
    POSTGRES = 'postgres',
    MYSQL = 'mysql',
    MARIADB = 'mariadb',
    ORACLE = 'oracle',
} 

export enum UserState {
    INACTIVE = 'inactive',
    ACTIVE = 'active',
    PENDING = 'pending'
}